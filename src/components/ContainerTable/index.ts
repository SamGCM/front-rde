import { styled } from "../../theme";
import { Box } from "../Box";

export const ContainerTable = styled(Box, {
    overflowX: "auto",
    padding: "$1",
    borderRadius: "$2",
    borderColor: "$celestial",
    borderStyle: "$solid",
    borderWidth: "$1",
    height: "fit-content",
}) 