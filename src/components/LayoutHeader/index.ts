import { styled } from "../../theme";
import { Box } from "../Box";

export const LayoutHeader = styled(Box, {
    padding: "$10 0px",
    marginBottom: "$10",
    borderBottomColor: "$celestial",
    borderBottomStyle: "$solid",
    borderBottomWidth: "$1",
    fontSize: "$8",
    fontWeight: "$medium"
})